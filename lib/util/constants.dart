import 'package:flutter/material.dart';

const paddingHorizontal48 = EdgeInsets.symmetric(horizontal: 48);
const paddingVertical48 = EdgeInsets.symmetric(vertical: 48);
const duration2 = Duration(seconds: 2);
const duration1 = Duration(seconds: 1);
const duration05 = Duration(milliseconds: 500);
